student = ["Pradeep", 21, "Nepal"]
print(student)

#Note : Strings are immutable but lists are mutable. So, we can change the value of list elements.


name = "Ram"
name[0] = "Sh" #This is not possible because strings are immutable
print(name)
